// Placeholder formatting utilities

export function formatDate(date: string | Date): string {
  throw new Error(`formatDate: not implemented — date: ${date}`);
}

export function formatLeadScore(score: number): string {
  throw new Error(`formatLeadScore: not implemented — score: ${score}`);
}

export function formatPercentage(value: number, decimals = 1): string {
  throw new Error(`formatPercentage: not implemented — value: ${value}, decimals: ${decimals}`);
}

export function formatCurrency(value: number, currency = 'USD'): string {
  throw new Error(`formatCurrency: not implemented — value: ${value}, currency: ${currency}`);
}

export function formatRelativeTime(date: string | Date): string {
  throw new Error(`formatRelativeTime: not implemented — date: ${date}`);
}
